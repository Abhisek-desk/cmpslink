# CAMPUSLINK Architecture

## Flow

Student/Recruiter/Admin
→ React frontend
→ Express API
→ MongoDB

AI-enabled flows:
- Resume → extraction → structured student profile
- Student profile + assessment data → readiness model
- Job description + student profiles → eligibility + semantic matching
- Matching results → explainability
- Historical outcomes → at-risk prediction
- Drive records → conflict detection
